FROM node:16.13.2-alpine as build

WORKDIR /server

ARG PORT DB_URL USER ADMIN ENCRYPT_KEY SECRET_KEY
ENV PORT={PORT} DB_URL={DB_URL} USER={USER} ENCRYPT_KEY={ENCRYPT_KEY} SECRET_KEY={SECRET_KEY}

COPY . /server
RUN yarn
CMD yarn start